pnpm-lock.yaml

# Don't format markdown, YAML, or Dockerfiles — prettier is slow on large md files
*.md
*.yml
*.yaml
Dockerfile
Dockerfile.*
**/Dockerfile

# Generated build output — never hand-edited, reformatting is pointless and slow
packages/*/dist/**
packages/ui/build/**
