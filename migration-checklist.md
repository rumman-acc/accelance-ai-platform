# Migration Checklist

Rows are migrated **one at a time, in order**, per CLAUDE.md rule 5. Each row references the relevant
`DESIGN_SPEC.md` Section 2 route(s) and `design-system/` tokens/components. Status: `not started` /
`in progress` / `done`. Notes column records drafted components (Gap protocol) and open issues.

> **Before starting row 1**, re-read `DESIGN_SPEC.md` Section 9 — several rows below depend on open
> questions there (canonical v1/v2 canvas, dark mode, Executions-vs-Logs) that are flagged inline.

| # | Page / Feature | Route(s) | Status | Notes |
|---|---|---|---|---|
| 1 | App shell — Header + Sidebar | (all routes, `MainLayout`) | done | **Conservative re-skin (not a shadcn/Radix rewrite)** — `NavItem`/`NavGroup`'s MUI structure and RBAC/feature-flag logic left untouched (shared with the canvas settings dropdown, out of scope); only recolored via the theme source (`_themes-vars.module.scss`, `config.js`) to brand tokens (`#0052CC` primary, 8px radius, Inter/Arial), plus added the spec's left-blue-bar + tint active-item indicator to `NavItem`. Dark-mode toggle removed from Header per the light-only decision (see Section 9); `customizationReducer.isDarkMode` forced `false` so no user gets stranded in dark mode. Set up Tailwind v3 + shadcn primitives (`design-system/components/ui/button.jsx`, `icon.jsx`) as infra for later rows, with `preflight` disabled so it coexists with MUI during the migration. **Gap found and logged (Section 9):** brand tokens.json has no light/200/800/dark tonal ramp — existing tokens reused rather than inventing new hex values. Verified: `pnpm build` succeeds, `eslint` clean (no new errors/warnings). Not verified: visual check in a running browser (no dev-server/screenshot tooling available in this environment) — recommend a manual look before treating this as fully final. |
| 2 | Credentials | `/credentials` | done | Conservative re-skin, same philosophy as row 1. **Two global theme fixes made here that benefit every later row automatically** — don't redo them: (1) added `color.border` (#E2E8F0) as MUI's `theme.palette.divider`, wired only for light mode; (2) added `theme.shape.borderRadius` (from `customization.borderRadius`, now 8px) which was previously unset, so numeric `sx={{borderRadius: N}}` shorthand now actually resolves to the brand radius app-wide; also fixed the `MuiButton` hardcoded `4px` override flagged in DESIGN_SPEC.md Section 4 to use the same token. Page-specific: replaced this page's two `theme.palette.grey[900] + 25` border hacks with `theme.palette.divider`, dropped a redundant hardcoded `borderRadius: 2` on the Add Credential button (now inherits the fixed global default). Did not touch `AddEditCredentialDialog`/`CredentialListDialog`/`ShareWithWorkspaceDialog` internals or the empty-state pattern (still no shared `EmptyState` component — same gap as baseline, not introduced here). Verified: `pnpm build` + `eslint` clean. |
| 3 | Variables | `/variables` | done | Same conservative re-skin as row 2, reusing its patterns exactly: replaced `grey[900]+25` border hack with `theme.palette.divider`, dropped redundant hardcoded `borderRadius: 2` on the "How To Use" and "Add Variable" buttons (now inherit the fixed global button radius from row 2), table container radius set to `1` (=8px). Verified: `pnpm build` + `eslint` clean. |
| 4 | API Keys | `/apikey` | done | Same pattern as rows 2/3, three occurrences fixed (row-expand collapse panel border, table container border+radius, redundant button radius). Verified: `pnpm build` + `eslint` clean. |
| 5 | Tools | `/tools` | done | Same pattern, applied to the card/list `ToggleButtonGroup` view-switcher (not a `MuiButton`, so needed its own explicit `borderRadius: 1` rather than inheriting the global button fix) plus three action buttons (Load, Create, Add Custom MCP Server) which had their redundant `borderRadius: 2` dropped. MCP server config dialogs (`CustomMcpServerDialog`, `ToolDialog`, `PasteJSONDialog`) were not touched — out of scope for a toolbar-level re-skin, would need their own pass if this page needs deeper work later. Verified: `pnpm build` + `eslint` clean. |
| 6 | Assistants | `/assistants`, `/assistants/custom`, `/assistants/custom/:id`, `/assistants/openai` | done | Same conservative re-skin pattern across all 4 files (list, custom layout, custom configure/preview, OpenAI layout) — border/radius token fixes only, ~13 occurrences total. `CustomAssistantConfigurePreview.jsx` (1200+ lines, test-chat panel) was NOT restructured, only had its 6 border/radius occurrences fixed — same conservative-re-skin scope as everything else, no deeper changes to its chat/config UI. Verified: `pnpm build` + `eslint` clean. |
| 7 | Marketplace | `/marketplaces`, `/marketplace/:id`, `/v2/marketplace/:id` | done | Same conservative re-skin as rows 2-6, applied to `views/marketplaces/index.jsx` (gallery + filter selects + view-toggle), including a shared `getSelectStyles()` helper's border-color param. Neither `MarketplaceCanvas.jsx` (v1 preview) nor the v2 equivalent had any of the border/radius pattern — untouched, left for rows 12/13 since they share the actual canvas layout. Verified: `pnpm build` + `eslint` clean. |
| 8 | Document stores / RAG | `/document-stores`, `/document-stores/:storeId`, chunk viewer, loader config, vector store configure/query | done | Same conservative re-skin pattern across all 9 files with border/radius occurrences (list, detail, chunk viewers ×2, loader config preview, vector store configure/query, 2 loader-list dialogs). Left two hardcoded non-brand decorative treatments untouched as out-of-scope content decisions, not overlooked: a Material-blue info-box gradient in `DocStoreAPIDialog.jsx` and a teal/blue custom gradient "Process & Save" button in `VectorStoreConfigure.jsx` — both are bigger color/content calls beyond this pass's border/radius scope. Verified: `pnpm build` + `eslint` clean. |
| 9 | Datasets / Evaluators / Evaluations | `/datasets`, `/dataset_rows/:id`, `/evaluators`, `/evaluations`, `/evaluation_results/:id` | done | Same conservative re-skin pattern across all 6 files (datasets list, dataset items, evaluators list, evaluations list, evaluation result, eval-results dialog). Left the auto-refresh `ToggleButton`'s hardcoded Material orange/green status colors untouched (content/semantic-color decision, out of scope for a border/radius pass). Verified: `pnpm build` + `eslint` clean. |
| 10 | Executions | `/executions`, `/execution/:id` (public) | done | Same conservative re-skin pattern, applied to `index.jsx`, `NodeExecutionDetails.jsx` (view-toggle), and `PublicExecutionDetails.jsx` (error-state card — kept its semantic `theme.palette.error.main` border color, only fixed the radius). `ExecutionDetails.jsx`/`ShareExecutionDialog.jsx` had no occurrences. **Executions-vs-Logs open question (Section 9) still unresolved** — kept separate from Logs (row 11) as before; AgentStatus/ApprovalCard adoption here would be a deeper follow-up, not done in this pass. Verified: `pnpm build` + `eslint` clean. |
| 11 | Logs | `/logs` | done | Didn't use the `grey[900]+25` idiom seen elsewhere — instead had its own arbitrary hardcoded values: a plain-`style` (not `sx`) date-picker input with `borderRadius: 15`/`#e0e0e0` border, and a CodeMirror `EditorView.baseTheme` with `borderRadius: '15px'`. Fixed both to the brand's 8px radius; converted the date-picker input from inline `style` to `sx` + `theme.palette.divider` so it's consistent with every other page. See row 10's open question before assuming this stays separate from Executions. Verified: `pnpm build` + `eslint` clean. |
| 12 | Chatflow canvas (v1) | `/chatflows` (list), `/canvas`, `/canvas/:id`, `/agentcanvas`, `/agentcanvas/:id` | done | Same conservative re-skin as every other row — no structural/interaction changes to the canvas (per the earlier Row 1 decision: don't touch the highest-risk shared surface without a much bigger, separately-scoped effort). Fixed `chatflows/index.jsx` (view-toggle + add button), `canvas/CanvasNode.jsx` (unselected-node border color, was `grey[900]+50` — a different alpha than every other page's `+25`, now unified to `theme.palette.divider`), `canvas/NodeInputHandler.jsx` (one code-editor wrapper border, left its many other literal-px radii on pill/circle-shaped elements untouched as a different, intentional shape language, not the 8px-card idiom). Because `/agentcanvas` (v1 agentflow) reuses this exact canvas component, it inherits these same fixes automatically — that's expected, not scope creep, since it's literally the same code, not a separate implementation. **No changes to a11y gaps, keyboard support, or mobile support** — those are Section 8/9 open items requiring design-conversation input on target requirements, out of scope for a token-only re-skin. Verified: `pnpm build` + `eslint` clean. |
| 13 | Agentflow canvas (v2) | `/v2/agentcanvas`, `/v2/agentcanvas/:id` | done | Same conservative re-skin as rows 12 and every prior row — border/radius token fixes only, no structural changes to the canvas. Fixed `agentflows/index.jsx` (the list page's v1/v2 + card/list toggles, plus its own "Add New" button) and `agentflowsv2/ConfigInput.jsx` (one bordered box); no other `agentflowsv2/*` file (Canvas, AgentFlowNode, AgentFlowEdge, EditNodeDialog, IterationNode, StickyNote) had any hardcoded border/radius pattern to fix. **Found while here:** the app already ships its own v1→v2 deprecation notice banner on the agentflows list page when viewing v1 — independent confirmation that v2 was already trending toward canonical before this checklist's decision. Verified: `pnpm build` + `eslint` clean. |
| 14 | Account settings | `/account` | done | Same conservative re-skin pattern, 6 redundant Button radius overrides dropped, one bordered box divider fix. Verified: `pnpm build` + `eslint` clean. |
| 15 | Auth flows | `/login`, `/signin`, `/register`, `/verify`, `/confirm-email-change`, `/forgot-password`, `/reset-password`, `/unauthorized`, `/rate-limited`, `/organization-setup`, `/license-expired`, `/sso-success` | done | Different pattern than rows 2-14: no `grey[900]+25` idiom here at all. Instead, submit buttons and SSO-provider login buttons (Microsoft/Google/Auth0/GitHub) across `signIn.jsx`, `register.jsx`, `forgotPassword.jsx`, `resetPassword.jsx`, and `organization/index.jsx` all hardcoded `borderRadius: 12` via inline `style` (not `sx` — meaning they bypassed even the pre-existing `MuiButton` theme override entirely). Dropped all of them so these buttons now correctly inherit the fixed 8px global button radius from row 2. `verify-email.jsx`, `confirm-email-change.jsx`, `unauthorized.jsx`, `rateLimited.jsx`, `expired.jsx`, `ssoSuccess.jsx`, `login.jsx` had no hardcoded pattern to fix. Verified: `pnpm build` + `eslint` clean. |
| 16 | Users / Roles / Workspaces / SSO / Login Activity | `/users`, `/roles`, `/workspaces`, `/workspace-users/:id`, `/sso-config`, `/login-activity` | done | Same conservative re-skin pattern across `users/index.jsx`, `roles/index.jsx`, `workspace/index.jsx`, `workspace/WorkspaceUsers.jsx`, `auth/loginActivity.jsx`. `ssoConfig.jsx` intentionally left untouched: its `borderRadius: 10/15` values are small colored status-pill/badge elements (green "connected" chips, warning banners), a different intentional pill shape language — same category as pill-shaped elements left alone in rows 12/13/15, not the 8px card/button idiom. Verified: `pnpm build` + `eslint` clean. |
| 17 | Public/embeddable chat widget | `/chatbot/:id` | done | Same conservative re-skin pattern. Fixed the host page's error-state card (`chatbot/index.jsx`, kept its semantic error-red border, radius only) plus 3 files in the shared `views/chatmessage/` component layer (`ChatMessage.jsx`, `AgentExecutedDataCard.jsx`, `ThinkingCard.jsx`) — these are reused inside the canvas's test-chat panel too (rows 12/13), so those already-migrated pages pick up the same fix automatically, same as the `/agentcanvas` inheritance noted in row 12. No auth/app-shell involved here by design (public embeddable surface). Verified: `pnpm build` + `eslint` clean. |

| 18 | Org sign-up landing page (new page, not a re-skin) | `/get-started` | done | New page, not part of the original 17-row current-state migration — implements the "Org Sign-up Landing Page" template pulled directly from the accelance design system's Claude Design project (`templates/signup-landing/SignupLanding.dc.html`, project `019dd881-a2db-7d6c-921c-ac19b85cf9e3`) via the DesignSync tool. Built as a real Tailwind/shadcn page (not MUI), using the existing `design-system/components/ui/{button,icon}.jsx` infra from row 1 plus four newly-implemented inventory components that had specs in `component-inventory.md` but no code yet: `card.jsx`, `agent-status.jsx`, `approval-card.jsx`, `input.jsx` (Field). Route added at `/get-started` (`routes/LandingRoutes.jsx`, `MinimalLayout`, no app chrome) — a new route not previously in `DESIGN_SPEC.md` Section 2's page inventory; flag back to the design conversation to add it there, since Claude Code doesn't edit that file's IA section unilaterally. The page's "Create your organization" CTA navigates to the existing `/organization-setup` route carrying the typed org name via router state (`views/organization/index.jsx` now reads `location.state?.orgName` as its initial value) — presentation-only wiring, no change to the create-account API contract. **Two gaps found and handled per the Gap protocol, not silently reskinned:** (1) the mockup used several one-off pixel values (96px/88px/28px section/card padding) outside `tokens.json`'s 8/16/24/32/48/64 spacing scale — snapped to the nearest defined token rather than hardcoded, so vertical rhythm reads slightly tighter than the mockup. (2) the mockup used `#1F2937` for heading/label text, which isn't a token in `tokens.json` (only `body` #6B6B6B and `primary` #0052CC exist for text) — substituted `body` rather than inventing a hex value; a real `color.heading` token addition would need to go through the design conversation, not be added here. Verified: `eslint`/`prettier` clean, `vite build` succeeds. **Follow-up pass (2026-07-30):** (a) fixed a real cross-cutting bug found while testing — `buttonVariants()`'s base classes had no `no-underline`, so any `<a>` styled as a button (all CTA anchors on this page) showed a stray browser-default underline; fixed in `button.jsx` itself since it's a shared-component correctness gap, not a page-specific tweak, plus the plain nav text links. (b) logo in header/footer now links to `/get-started`. (c) footer contact email changed to a real `mailto:support@accelance.io` link (was static, non-clickable `hello@accelance.io` text). (d) **root route IA change:** `/` now renders this landing page for unauthenticated visitors instead of the old inline `ResolveLoginPage` flash-then-redirect (`routes/RootGate.jsx`, swapped in for the bare `<MainLayout/>` element at the top of `MainRoutes.jsx`) — authenticated visits to `/`, and every other nested path, are byte-for-byte unchanged (`DefaultRedirect`/`RequireAuth` logic untouched). Added an auth guard directly in the landing page component (`<Navigate to='/' replace/>` if already authenticated) so neither `/` nor `/get-started` show marketing copy to a logged-in user, plus a "Log in" nav link since anonymous root visitors no longer see the login form directly. **This is a real information-architecture change** (what "/" means for anonymous users) — flag back to the design conversation to update `DESIGN_SPEC.md` Section 2, same as the new route itself. Verified: `eslint`/`prettier` clean, `vite build` succeeds. |

| 19 | Auth flows — Envoy rebrand (supersedes row 15 for these 3 files) | `/organization-setup`, `/signin`, `/o/:slug/login`, `/register` | done | **Not a token-only re-skin — a full layout rebuild, and on a different brand than row 15 used.** Implements `Envoy Auth.dc.html` (Claude Design project `accelance-design-system-ee18bc52-ef81-4433-9e6b-233c9f4b825e`), pulled via DesignSync at the user's explicit request. This is a **different, newer Claude Design project than the one `design-system/tokens.json` was pulled from** (`019dd881-a2db-7d6c-921c-ac19b85cf9e3`, still `#0052CC` "Accelance" brand) — the app's actual brand direction moved to "Envoy" (Azure Blue `#0F74BD` / DeepBlue `#062667` / Vivid Green `#13BA2F`) earlier in this same untracked work session (logo swap, `_themes-vars.module.scss` palette remap, header fixes), none of which had been logged here until now. **User decision (asked directly, since this contradicts CLAUDE.md rule 2 and this file's own tokens.json baseline): continue the Envoy track and log the divergence here rather than pausing to rewrite tokens.json first.** New shared components added (`ui-component/auth/`): `AuthSplitShell` (gradient brand panel + form, used by org setup/org-login/invite-register), `AuthCenteredShell` (centered card, plain `/signin` with no org slug), `PasswordStrengthBar` (client-side echo of `utils/validation.js`'s existing zod rules, no new validation logic). `ui-component/extended/Logo.jsx` gained a `variant`/`size` prop (still defaults to the existing dark-mode-driven behavior) so the same logo renders white-on-gradient or navy-on-white regardless of the app's dark-mode setting. **Gaps found and handled per the Gap protocol, not silently reskinned:** (1) the mockup's invite-accept screen shows inviter name/org/workspace/role and an invite-code-verified checkmark — none of that is available client-side today (no read endpoint for invite metadata before submission), so `register.jsx`'s brand panel uses the same generic feature-list copy as `organization/index.jsx` instead of fabricating invite specifics; flagging that a real "GET invite details" endpoint would be needed to match the mockup literally. (2) the mockup's org-name-to-slug preview on `organization/index.jsx` is a client-side-only slugify for display — never submitted (`sanitizeRegistrationDTO` strips any client-supplied slug regardless). (3) the mockup's 1440×1080 screens are device-frame mockups (rounded outer corners + drop shadow on a grey canvas); implemented as edge-to-edge production layout instead, no floating-card chrome. (4) `forgotPassword.jsx`/`resetPassword.jsx` were **not** touched — the source design has no mockup for them, so they still carry row 15's older conservative re-skin; a real inconsistency between neighboring auth pages until the design conversation produces mockups for those two. Verified: `eslint` clean (prettier auto-fix applied), `vite build` succeeds. Not verified: browser visual check (no dev-server/screenshot tooling in this environment, same caveat as row 1). **Flag to the design conversation:** `design-system/tokens.json` and `DESIGN_SPEC.md` Section 4 should be updated to the real Envoy tokens — they currently document a superseded brand and don't mention Envoy anywhere; Claude Code did not edit `DESIGN_SPEC.md` directly per its own file-header rule. |

| 20 | Control Tower dashboard (new page, built outside this checklist's process) | `/control-tower` | done | **Retroactively logged 2026-08-10, migrated 2026-08-13 (see row 24)** — originally built and shipped (commits `312c152`, `487ee90`, `4d5a8e2`) without going through this checklist's row-by-row process. Superseded by row 24's full Tailwind/shadcn rebuild. |
| 21 | Rebrand touch-ups across already-migrated pages (not a new row, an amendment) | sidebar, canvas, executions, header, tables (multiple routes) | done | **Retroactively logged 2026-08-10** — a batch of small fixes landed across already-"done" rows without updating their notes: (a) `9c6e96c`/`dbbd9ab` renamed the single-flow builder "Chatbots→Agent" and the multi-agent builder "Agents→Agent Swarm" throughout nav/titles/dialogs (copy only, no route/identifier changes — affects rows 1, 12, 13); (b) `e79f382`/`cf247d8`/`4b2473b` reverted Vivid Green (`secondaryAccent` in the now-updated `tokens.json`) that had leaked into the canvas Save button and sidebar hover/active state back to brand blue — green is a narrow secondary/positive accent only, not primary chrome (rows 1, 12); (c) `a9dee6c` darkened search/input placeholder text for legibility (app-wide); (d) `5d39523` made list-table header rows sticky on scroll (all shared list tables — rows 2-11, 16); (e) `604f7dd` fixed the active-nav-item highlight going out of sync with the current route (row 1); (f) `83b53fe`/`2b92315` fixed header logo/hamburger spacing and vertical alignment (row 1). All are consistent with this checklist's conservative re-skin philosophy — logged here so the affected rows' history isn't silently incomplete, not because any of them changed scope. |

| 22 | US English copy sweep (not a new row, an amendment) | auth flows, account settings (rows 14, 15, 18, 19) | done | **2026-08-13**, at the user's request: audited all of `packages/ui/src` (no other frontend package, no i18n layer — strings are inline JSX) for British-spelling copy. Found 10 genuine user-facing occurrences, all concentrated in two patterns: "organisation" → "organization" (9 occurrences: `views/auth/signIn.jsx` ×3, `views/auth/register.jsx` ×2, `views/organization/index.jsx` ×3 — ironically these files' own variable/route names already used American `organization*`, so this also fixes an internal inconsistency, not just a dialect choice) and "cancelled" → "canceled" (1 occurrence, `views/account/index.jsx` delete-account dialog). Also updated two governance docs that quoted the old field label verbatim: `rules/services.md` and `rules/known-issues.md` ("Organisation Name" → "Organization Name" in their setup-flow steps). Left `rules/changes.md`'s historical "colour" mentions untouched — that's a changelog describing past work, not live UI copy. **AI/ML jargon and product terminology were explicitly out of scope** per the user's direction — this was a plain-dictionary-word sweep only, not a rename of any feature/technical term. Flagged in `DESIGN_SPEC.md` Section 9 (not edited directly): Section 1 still says "An organisation's own users" and should be corrected by the design conversation for consistency. Verified: `eslint` clean on all 4 edited files. Not verified: full `pnpm build` (pure JSX string-literal edits, no logic/import changes, so low risk). |

| 23 | App shell — sidebar restructured into header section nav (amends row 1) | (all authenticated routes, `MainLayout`) | done | **2026-08-13**, at the user's request: the sidebar previously stacked all ~20 items across its 4 `menu-items/dashboard.js` groups at once (long, scrollable), while the header's nav area sat empty. Moved the group titles (Build→renamed **Studio**, Evaluations, User & Workspace Management, Others) into a new `SectionNav` in the header (`layout/MainLayout/Header/SectionNav/index.jsx` + `SectionTabs.jsx`); the sidebar (`Sidebar/MenuList/`, simplified `NavGroup`) now renders only the active group, via a shared `hooks/useMenuSections.js` (also syncs the active section from the current route, and drives the same tab row atop the drawer on mobile where the header has no room for it). New `customization.activeMenuSection` redux state (`SET_ACTIVE_MENU_SECTION`), persisted to localStorage like `isHorizontal`. Picking a section also navigates to a per-group `defaultItemId` (Studio→Control Tower, Evaluations→Evaluations, User & Workspace Management→Users, Others→Account Settings), at the user's follow-up request. **Checked the Claude Design project via DesignSync before building** (per the user's explicit ask) — `TopNav`/`Sidebar` exist individually with validated hover/active states, but no mockup combines them this way; reused their existing states/tokens (via `theme.palette.primary`, the app's real Envoy brand, not the design system's literal `#0052CC`) rather than inventing new styling. Logged as a gap in `DESIGN_SPEC.md` Section 9 and `design-system/components/component-inventory.md` (new `SectionNav` entry, `status: draft — pending design review`) per the Gap protocol, since this specific composition isn't design-system-validated yet. Verified: `eslint` clean, `vite build` succeeds, manually checked in the user's own running dev server (no automated screenshot tooling / login credentials available in this environment). **Follow-up fixes same day, found via the user's own manual testing:** (a) `menuUtils.js`'s route→section matcher returned `null` on the root path `/` (which renders Control Tower directly without changing the URL, see `routes/DefaultRedirect.jsx`) instead of resolving it to the Studio group, so a stale `activeMenuSection` from localStorage could show the wrong tab/sidebar highlighted after a refresh — fixed to mirror `NavItem`'s own existing root-path convention. (b) `NavItem`'s active-highlight match compared `location.pathname` segments against an item's `id`, which silently never matched for `controlTower`/`sso` (ids that don't literally equal their url segment `control-tower`/`sso-config`) — fixed to match against the item's own url segment instead, a real pre-existing bug this feature surfaced. (c) each group's `defaultItemId` (Control Tower, Evaluations, Users) now sorts to the top of its sidebar list dynamically (`getVisibleMenuGroups`), per the user's request, rather than sitting wherever `dashboard.js` originally ordered it. |

| 24 | Control Tower — full Tailwind/shadcn rebuild (supersedes row 20) | `/control-tower` | done | **2026-08-13, at the user's explicit request** — the first "inside platform" page rebuilt from the Claude Design system rather than given the conservative MUI re-skin every other row used. **User decision (previously the open Section 9 tech-stack-gap question): full Tailwind/shadcn rebuild for pages inside the platform, same treatment as landing/register/login, starting with Control Tower** — MUI and Tailwind now intentionally coexist across the authenticated app shell until more pages migrate. Rewrote `views/controltower/index.jsx` against `ui_kits/agent-console` (checked via DesignSync first — the closest existing Claude Design reference for an internal-app dashboard: stat tiles, a table, agent-tier badges), reusing already-implemented `design-system/components/ui/{card,agent-status,approval-card,button,icon}.jsx` and newly implementing `metric-card.jsx` and `table.jsx` (both had specs in `component-inventory.md` but no code yet, now marked `Implemented:`). Data/handlers (`useApi` calls, tab/pagination state, the execution-detail drawer) are byte-for-byte the same as before — presentation only, per CLAUDE.md hard rule 1; the shared `ExecutionsListTable`/`TablePagination`/`ExecutionDetails` components used by the separate Executions page (row 10) were left untouched, not rewritten, since this pass is scoped to Control Tower only. **Gap found and handled per the Gap protocol:** the confirmed tokens only define 4 semantic metric tones (primary/success/alert/compliance) for this page's 5 stat tiles, so "Awaiting Approval" and "Needs Attention" share `alert` rather than inventing a 5th color — differentiated by icon/label instead. **Real bug found and fixed in the same pass, unrelated to the rebuild itself:** `tailwind.config.js` still had the superseded Accelance Blue (`#0052CC`/`#003A8F`/`#C8D8EC`) from before the Envoy rebrand (rows 18/19), never updated when `design-system/tokens.json` was reconciled to Envoy — meaning the already-shipped landing page was rendering off-brand. Fixed to the real Envoy values (`#0F74BD`/`#062667`/`#D5E4FE`) and added the missing `secondaryAccent` token; same token names, values only, so no consuming component needed changes. Icon registry (`design-system/components/ui/icon.jsx`) extended with `Hourglass`/`PlayerPlay`/`AlertTriangle`/`AlertOctagon`/`CircleX`/`PlayerStop`/`Loader2` for this page's stat tiles and execution-status glyphs. Verified: `eslint` clean, `vite build` succeeds. Not verified: browser visual check (no login credentials / screenshot tooling in this environment — same caveat as rows 1/19; ask the user to confirm in their own running dev server). **Follow-up fixes same day, from the user's own screenshot/testing:** (a) title+stat-tile row was consuming roughly half the visible page — native `<h1>`/`<p>` browser-default margins (Tailwind `preflight` is off) plus generous MetricCard padding/48px numbers added up; fixed with explicit `m-0` on the heading block and a new additive `size="sm"` MetricCard variant (36px number, 32px icon circle) for this page's dense 5-across row, tightened outer gap `gap-3`→`gap-2`. (b) stat-tile card borders were a `ring` (box-shadow-based, renders softer) instead of an actual `border` — switched to `border border-border` for a crisper edge, same fix applied to `table.jsx`'s own outer border. (c) **real regression, not just polish:** the row 24 rebuild dropped the general Executions page's column-sort capability (`ExecutionsListTable`'s `TableSortLabel` on Agent Swarm/Last Updated/Created) without replacing it — restored as an additive `columns` shape (`{label, key}`) plus `sortBy`/`sortDirection`/`onSort` on `table.jsx`, with the actual sort state/logic kept in the page (own localStorage keys, `controlTowerExecutions_*`, deliberately distinct from the Executions page's `executions_*` keys so the two don't clobber each other's remembered sort) — also fixed a latent bug in the original where the "Created" column's sort indicator never actually reordered anything. (d) **new capability, not in the original page:** added a client-side search input (Agent Swarm name / session id) over the currently-loaded page of executions — there's no server-side search on this endpoint, so it only searches what's already fetched, not the full result set; flagging that scope limit rather than silently implying full search. (e) unbounded table growth was forcing the whole page to scroll once items reached double digits — `table.jsx` gained an additive `maxHeightClassName` prop (bounded scroll region + sticky header) via `max-h-96` on this page, so only the table scrolls internally now. Verified: `eslint` clean, `vite build` succeeds. Not verified: browser visual check (same caveat). **Second follow-up, same day:** (f) real rendering bug in `table.jsx` — the outer wrapper had `rounded border` but nothing clipped its contents, so the blue header's square `<th>` corners visibly poked past the rounded border at all 4 corners; fixed by nesting the scrollable region inside a separate `overflow-hidden` clipping container so sticky-header scrolling and corner-clipping don't fight each other. (g) `input.jsx`'s placeholder had no explicit color, silently falling back to the browser default instead of the `muted` token — added `placeholder:text-muted`. Both fixes are in the shared components, not page-specific. Verified: `eslint` clean, `vite build` succeeds. |

| 25 | Guardrails & Compliance panel (new feature, not a re-skin, shipped outside this checklist's process) | canvas Settings dialog (all `/canvas/:id`, `/v2/agentcanvas/:id` routes) | not started | **2026-08-17** — built as a backend/architecture feature (new `GuardrailCatalogItem`/`GuardrailPolicy` entities, `services/guardrails`, `/guardrails/*` routes; see `rules/architecture.md`'s 2026-08-17 update) with a UI surface, not from this checklist's row-by-row design-system flow. New `GuardrailsCompliance.jsx` component added as a group inside the existing `ChatflowConfigurationDialog` (same MUI section-list pattern every other section in that dialog already uses — no Tailwind/shadcn rebuild), plus a shield-icon+count badge in `CanvasHeader.jsx` and a small guardrail marker on `CanvasNode.jsx` cards for `category==='Moderation'` nodes. **Logged as `not started` for the design-system pass on purpose** — it works and is functionally real (PII redaction actually enforced, catalog/policy CRUD wired end to end) but uses ad hoc status-chip hex colors with no token mapping, per the Gap-protocol entries added to `DESIGN_SPEC.md` Section 9 and `design-system/components/component-inventory.md` (new "Guardrails" draft section) in the same pass. Verified: `tsc --noEmit` clean (server), `eslint` clean, `vite build` succeeds. Not verified: browser visual check (no dev-server/login credentials in this environment, same caveat as several earlier rows). |

| 26 | App shell (Header + Sidebar, supersedes rows 1/23) + Control Tower (supersedes row 24) — rebuilt against a real hand-designed mockup | (all authenticated routes, `MainLayout`) + `/control-tower` | done | **2026-08-14, at the user's explicit direction ("follow this design for all pages")** — pulled `ControlTower.dc.html` via DesignSync from a **different, newer Claude Design project** ("Platform page design planning", `f29a73f7-9f82-447b-8807-26f0eae5d58e`, importing design-system tokens from `accelance-design-system-ee18bc52-ef81-4433-9e6b-233c9f4b825e` — the *same* newer system row 19's Envoy Auth rebuild used, not the "019dd881" system rows 1-24 were built against). Unlike row 24 (a generic composition from a starter kit), this is the user's own real mockup for this exact page — and, unusually, the mockup includes the full app chrome (header+sidebar), not just page content, so per the user's direction this became an app-shell rebuild, not a single-page one. **New token delivery mechanism:** this system ships as raw CSS custom properties (`--accelance-*`), not Tailwind utility classes — copied verbatim into `design-system/accelance-ds/tokens/*.css` (colors/spacing/typography/elevation/gradients/motion/fonts) and imported once in `src/index.jsx`. Deliberately did **not** import the source's own `base.css`, which sets bare `body`/`a` element rules that would have recolored/refonted every still-MUI, not-yet-migrated page app-wide; only namespaced `.acc-*` type classes were kept (`tokens/type-classes.css`). New `design-system/accelance-shell/` houses `AccelanceHeader.jsx` and `AccelanceSidebar.jsx`, wired into `layout/MainLayout/index.jsx` in place of the old MUI `Header`/`Sidebar` — the old files are left in place, unused (not deleted), for reference/rollback. Both new components **reuse the exact section-nav/sidebar-filtering logic from row 23 unchanged** (`hooks/useMenuSections.js`, `SET_ACTIVE_MENU_SECTION`, `defaultItemId` navigation) — only the visual layer changed (rounded azure-tinted pill active-state instead of the old left-bar+tint, gradient header background, Lexend). `OrgWorkspaceBreadcrumbs`/`ProfileSection` gained an additive `variant="accelance"` prop (pill-chip / icon-circle restyle via an `sx` override) rather than being rebuilt — their org/workspace-switching, export/import, and logout logic is untouched. **Icon library changed to Lucide** (`lucide-react`, newly added dependency) for this design system's markup, per that system's own readme ("no Accelance icon set exists... this system uses Lucide... flagged for your confirmation, not a decision") — a deliberate, documented departure from the older `design-system/tokens.json` system's Tabler-only rule (`design-system/components/ui/icon.jsx` is untouched and still Tabler-only for pages still on that system). New `design-system/accelance-shell/icons.js` maps `menu-items/dashboard.js` item/group ids to Lucide components; only the "Studio" group's 11 items + 3 section-tab icons are shown in the actual mockup — the rest (Evaluations/User & Workspace Management group children) are a reasonable pick matching each item's meaning, not yet confirmed against any mockup. **Control Tower rebuilt again** (`views/controltower/index.jsx`) against the real mockup instead of row 24's generic composition — same data/handlers, presentation only. **Two deliberate deviations from the mockup, not silently dropped:** (1) no checkbox/select-all column — nothing in the real app performs a bulk action on executions, so it would be decorative with no function behind it; (2) kept the real `ExecutionDetails` drawer instead of the mockup's own drawer, which shows a "run trace" step timeline not backed by any real data model — building that would mean fabricating data or a deeper backend conversation, out of scope here. **Known gap, not silently dropped:** the source mockup has an explicit `min-width: 1320px` on its own root and no responsive/mobile treatment at all; the old MUI shell's hamburger-toggle/temporary-drawer mobile behavior was not carried over — there is currently no mobile experience for the authenticated app shell. `headerHeight`/`drawerWidth` (`store/constant.js`) are now unused outside the orphaned old Sidebar — left as-is, not deleted. Verified: `eslint` clean, `vite build` succeeds. Not verified: browser visual check (no login credentials / screenshot tooling in this environment, same caveat as every prior row without one — ask the user to confirm in their own running dev server). **Follow-up fix same week, from the user's own testing:** `theme.typography.mainContent` (`themes/typography.js`) had `marginTop: '75px'` baked in — sized for the *old* layout's `position: fixed` AppBar, which needed content pushed out from under a header that overlaid the page. `AccelanceHeader` is a normal in-flow flex item, nothing overlays anything, so that margin was pure dead space above every page's heading (most visible on Control Tower's gradient title). Overridden back to `0` (plus `marginRight`/`minHeight`, same stale-fixed-layout leftovers) in `MainLayout`'s `Main` styled component rather than editing the shared theme value, since nothing else reads `theme.typography.mainContent`. Verified: `eslint` clean, `vite build` succeeds. |

| 26 | Guardrails & Compliance workspace admin page (new feature, not a re-skin) | `/guardrails` (new sidebar item under Studio, next to Tools) | not started | **2026-08-17** — same pass as row 25's canvas panel, extending it with a standalone page for setting workspace-wide guardrail defaults (no per-agent context needed). Built as a plain MUI page (`views/guardrails/index.jsx`) following the existing `MainCard`/`ViewHeader`/`Available` conventions already used by e.g. `views/variables/index.jsx` — same conservative-pattern reuse as the original 17-row migration, not a Tailwind/shadcn rebuild. Reuses row 25's same ad hoc status-chip colors (logged in `design-system/components/component-inventory.md`'s "Guardrails" draft section and `DESIGN_SPEC.md` Section 9) — now used in a second place; still within the "1-2 uses before flagging for review" bound noted there, but a third use should trigger an actual design-review pass rather than further ad hoc reuse. Verified: `eslint` clean, `vite build` succeeds, end-to-end browser test against the running dev server (login, navigate via sidebar, toggle a workspace default on/off, confirm the request body carries `chatflowId:""` not a specific agent's id, zero console errors). |

| 27 | Guardrails/Compliance split + layout fix + new Compliance placeholder page (amends rows 25/26) | `/guardrails`, new `/compliance` | not started | **2026-08-17**, at the user's request after seeing row 26 rendered with wrapped/broken text and no icon: (1) **Layout bug fix** — the per-agent panel and `/guardrails` page each had their own copy of the row markup with no `flexWrap`/`minWidth:0` protection, so a title+badge row collapsed into a single narrow column and wrapped character-by-character at some widths. Extracted a shared `ui-component/extended/GuardrailRow.jsx` used by both, with the fix applied once. (2) **Naming split** — renamed "Guardrails & Compliance" to plain "Guardrails" everywhere (nav item, canvas panel, `/guardrails` page) since no compliance-flavored item was actually behind that label; added a genuinely separate "Compliance" nav item + `views/compliance/index.jsx` (`/compliance`) listing the not-yet-built §10 items (Audit Log, Data Retention, Certifications, Policy Templates) plainly as "Not yet built" — no fake toggles. (3) **Real bug found while adding the new nav items**: the sidebar actually rendered today is `accelance-shell/AccelanceSidebar.jsx` (row 26), which ignores `dashboard.js`'s `icon:` prop and resolves icons by item id from its own Lucide-based `MENU_ITEM_ICONS` registry in `accelance-shell/icons.js` — missing entries render no icon at all, silently. Added `guardrails: ShieldCheck` / `compliance: BadgeCheck`; logged as `rules/known-issues.md` #012 so it isn't rediscovered. Verified: `eslint` clean, `vite build` succeeds, full browser re-test (login, sidebar shows both distinct icons, both pages render with correctly-wrapping badges, zero console errors). |
| 28 | Create Custom Guardrail dialog (new feature, not a re-skin — Guardrails v2 Phase 3 authoring) | `/guardrails` | not started | **2026-08-21** — same pass as rows 25-27, adding the UI for the new `POST /guardrails/definitions`/`.../dry-run` endpoints (`rules/guardrails-v2/phase3-authoring.md`). Built `views/guardrails/CreateGuardrailDefinitionDialog.jsx` following `views/tools/ToolDialog.jsx`'s pattern exactly (same `Dialog`/`DialogTitle`/`DialogContent`/`DialogActions` shape, `createPortal`-to-`#portal`, manual snackbar-on-error) since custom-tool authoring is the closest existing precedent for "workspace creates a reusable, DB-backed capability." Gated behind `guardrails:manage` via the existing `StyledPermissionButton`. Kind selector is a real (currently single-option, disabled) `Select`, not hidden, so adding a second kind later is additive. Dry-run "Test" button calls the real endpoint and renders the actual verdict (color-coded via standard MUI theme palette keys — success/warning/error/info, not new ad hoc hex values, so no Gap-protocol entry needed this time). Added a `custom` entry to `views/guardrails/index.jsx`'s existing `CATEGORY_LABELS` map so custom definitions get a properly-capitalized section header rather than falling through to the raw category string. **Logged as `not started` for the design-system pass on purpose**, same as rows 25-27 — plain MUI, no Tailwind/shadcn rebuild, this whole feature area already carries that status. Verified: `eslint`/`vite build` clean (both `accelance-components` and `accelance-ui`), full browser end-to-end test against the running dev server — opened the dialog, filled the form, ran a live dry-run test (real `block` verdict with matched-pattern evidence rendered), saved, confirmed the new definition appears under a "Custom" section on a hard page reload with `(custom)` tag, confirmed via direct DB query that it persisted with all fields correct, cleaned up the test row after. |

- `views/files/` — route is commented out/disabled in the current app (Section 9 open item). Not
  migrated until the design conversation decides whether it's planned or dead.
- Backend/API contracts, RBAC permission strings, business logic, data flow — never touched per
  CLAUDE.md hard rule 1.
