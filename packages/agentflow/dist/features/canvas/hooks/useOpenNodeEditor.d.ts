/**
 * Hook to open the node editor dialog
 * Handles fetching node data and schema, then opens the edit dialog
 */
export declare function useOpenNodeEditor(): {
    openNodeEditor: (nodeId: string) => void
}
